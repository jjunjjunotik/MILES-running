# VS Code 에서 시작하기

## 1. 폴더 열기
VS Code 를 켜고 **파일 → 폴더 열기** 로 이 폴더(`nailsense`)를 엽니다.

## 2. API 키 넣기
왼쪽 탐색기에서 **`.env`** 파일을 클릭합니다.
14번째 줄이 이렇게 되어 있습니다.

```
GEMINI_API_KEY=
```

`=` 바로 뒤에 키를 붙여넣고 저장(Ctrl+S 또는 Cmd+S)합니다.

```
GEMINI_API_KEY=AIzaSyD여기에실제키
```

- 따옴표는 넣지 마세요
- 줄 끝에 공백이 남지 않게 하세요
- 키 발급: https://aistudio.google.com/apikey

`.env` 가 안 보이면 탐색기에서 숨김 파일이 꺼져 있는 것입니다.
VS Code 는 기본적으로 보여 주지만, 안 보이면 `Ctrl+P` 를 누르고 `.env` 를 입력하세요.

## 3. 설치하고 실행
VS Code 에서 **터미널 → 새 터미널** 을 열고:

```bash
npm install
npm run dev
```

브라우저에서 http://localhost:5173 을 엽니다.

## 4. 키가 잘 들어갔는지 확인
터미널에 이 줄이 뜨면 성공입니다.

```
분석 프로바이더: Gemini (gemini-3.8-flash) · GEMINI_API_KEY 설정됨
```

대신 `GEMINI_API_KEY 가 없어 데모 응답으로 동작합니다` 가 뜨면 키가 안 읽힌 것입니다.
`.env` 를 다시 열어 따옴표나 공백이 섞이지 않았는지 확인하세요.
공백이 섞인 경우에는 `[보안] GEMINI_API_KEY 앞뒤에 공백이 있습니다` 경고도 함께 뜹니다.

## 어떤 파일을 고치면 되나
| 하고 싶은 것 | 파일 |
|---|---|
| API 키 넣기 | `.env` |
| AI 에게 주는 지시문 바꾸기 | `server/prompt.ts` |
| 분석 항목·결과 형식 바꾸기 | `shared/analysis.ts`, `server/schema.ts` |
| 화면 바꾸기 | `web/src/screens/` |
| 색·여백 등 디자인 | `web/src/styles.css` |

## 주의
`.env` 는 `.gitignore` 에 들어 있어 git 에 올라가지 않습니다.
키를 소스 코드에 직접 적지 마세요. 한 번 올라가면 폐기하고 재발급해야 합니다.
